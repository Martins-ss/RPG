run = "cd artifacts/rpg-companion && PORT=3000 BASE_PATH=/ pnpm dev --host 0.0.0.0"
modules = ["nodejs-20"]

[[ports]]
localPort = 3000
externalPort = 80

[agent]
expertMode = true

[nix]
packages = ["nano"]
